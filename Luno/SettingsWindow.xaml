<Window x:Class="Luno.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="設定" Height="320" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="0" GlassFrameThickness="-1" CornerRadius="8"/>
    </WindowChrome.WindowChrome>

    <Border x:Name="RootBorder" CornerRadius="8" BorderBrush="#40808080" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/> <!-- Title Bar -->
                <RowDefinition Height="*"/>  <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Border Grid.Row="0" x:Name="TitleBarBorder" CornerRadius="8,8,0,0">
                <Grid>
                    <TextBlock x:Name="TitleText" Text="設定" VerticalAlignment="Center" Margin="16,0,0,0" FontWeight="Bold"/>
                    <Button x:Name="CloseBtn" Content="&#xE8BB;" HorizontalAlignment="Right" Width="32" Height="32" 
                            WindowChrome.IsHitTestVisibleInChrome="True"
                            Click="CloseButton_Click" 
                            FontFamily="Segoe Fluent Icons"
                            Background="Transparent" BorderThickness="0" Cursor="Hand">
                        <Button.Style>
                            <Style TargetType="Button">
                                <Setter Property="Foreground" Value="#AAAAAA"/>
                                <Style.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter Property="Foreground" Value="#FFFFFF"/>
                                        <Setter Property="Background" Value="#E81123"/>
                                    </Trigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>
                </Grid>
            </Border>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,16">
                <StackPanel>
                    <!-- Theme Section -->
                    <TextBlock x:Name="ThemeLabel" Text="テーマカラー" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,12"/>
                    
                    <!-- 12 Color Circles -->
                    <WrapPanel x:Name="ThemeButtonsPanel" Margin="0,0,0,20">
                        <!-- Buttons added dynamically in code-behind -->
                    </WrapPanel>

                    <!-- Zoom Section -->
                    <TextBlock x:Name="ZoomLabel" Text="文字サイズ (ズーム)" FontSize="14" FontWeight="SemiBold" Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <Slider x:Name="ZoomSlider" Minimum="10" Maximum="500" TickFrequency="10" 
                                IsSnapToTickEnabled="True" ValueChanged="ZoomSlider_ValueChanged"/>
                        <TextBlock x:Name="ZoomValueText" Grid.Column="1" Text="100%" 
                                   HorizontalAlignment="Right" VerticalAlignment="Center"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
