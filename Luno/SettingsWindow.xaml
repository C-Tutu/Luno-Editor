<Window x:Class="Luno.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="設定" Height="320" Width="420"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="0" GlassFrameThickness="-1" CornerRadius="8"/>
    </WindowChrome.WindowChrome>

    <Border x:Name="RootBorder" CornerRadius="8" BorderBrush="#30808080" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/> <!-- Title Bar -->
                <RowDefinition Height="*"/>  <!-- Content -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0">
                <TextBlock Text="設定" VerticalAlignment="Center" Margin="16,0,0,0" 
                           Foreground="{Binding ElementName=RootBorder, Path=Tag}" FontWeight="Bold"/>
                <Button Content="&#xE8BB;" HorizontalAlignment="Right" Width="32" Height="32" 
                        Style="{StaticResource WindowCloseButton}" Click="CloseButton_Click" 
                        FontFamily="Segoe Fluent Icons"/>
            </Grid>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24,16">
                <StackPanel>
                    <!-- Theme Section -->
                    <TextBlock Text="テーマカラー" FontSize="14" FontWeight="SemiBold" 
                               Foreground="{Binding ElementName=TitleText, Path=Foreground}" Margin="0,0,0,12"/>
                    
                    <!-- 12 Color Circles -->
                    <WrapPanel x:Name="ThemeButtonsPanel" Margin="0,0,0,20">
                        <!-- Buttons added dynamically in code-behind -->
                    </WrapPanel>

                    <!-- Zoom Section -->
                    <TextBlock Text="文字サイズ (ズーム)" FontSize="14" FontWeight="SemiBold" 
                               Foreground="{Binding ElementName=TitleText, Path=Foreground}" Margin="0,0,0,8"/>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <Slider x:Name="ZoomSlider" Minimum="50" Maximum="200" TickFrequency="10" 
                                IsSnapToTickEnabled="True" ValueChanged="ZoomSlider_ValueChanged"/>
                        <TextBlock x:Name="ZoomValueText" Grid.Column="1" Text="100%" 
                                   HorizontalAlignment="Right" VerticalAlignment="Center" 
                                   Foreground="{Binding ElementName=TitleText, Path=Foreground}"/>
                    </Grid>
                </StackPanel>
            </ScrollViewer>
        </Grid>
    </Border>
</Window>
