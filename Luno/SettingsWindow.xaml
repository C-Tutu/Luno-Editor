<Window x:Class="Luno.SettingsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="設定" Height="400" Width="500"
        ResizeMode="NoResize"
        WindowStartupLocation="CenterOwner"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">

    <WindowChrome.WindowChrome>
        <WindowChrome CaptionHeight="32" ResizeBorderThickness="0" GlassFrameThickness="-1" CornerRadius="8"/>
    </WindowChrome.WindowChrome>

    <Border CornerRadius="8" Background="{DynamicResource BackgroundBrush}" BorderBrush="#30808080" BorderThickness="1">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="32"/> <!-- Title Bar -->
                <RowDefinition Height="*"/>  <!-- Content -->
                <RowDefinition Height="Auto"/> <!-- Bottom Buttons -->
            </Grid.RowDefinitions>

            <!-- Title Bar -->
            <Grid Grid.Row="0">
                <TextBlock Text="設定" VerticalAlignment="Center" Margin="16,0,0,0" Foreground="{DynamicResource TextBrush}" FontWeight="Bold"/>
                <Button Content="&#xE8BB;" HorizontalAlignment="Right" Width="32" Height="32" Style="{StaticResource WindowCloseButton}" Click="CloseButton_Click" FontFamily="Segoe Fluent Icons"/>
            </Grid>

            <!-- Content -->
            <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="24">
                <StackPanel>
                    <!-- Appearance Section -->
                    <TextBlock Text="外観" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,12"/>
                    
                    <!-- Theme -->
                    <TextBlock Text="カラーテーマ" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
                    <ComboBox x:Name="ThemeComboBox" Margin="0,0,0,16" SelectionChanged="ThemeComboBox_SelectionChanged" Height="30" VerticalContentAlignment="Center">
                        <ComboBox.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal">
                                    <Border Width="16" Height="16" CornerRadius="8" Background="{Binding BackgroundBrush}" BorderThickness="1" BorderBrush="#80808080" Margin="0,0,8,0"/>
                                    <TextBlock Text="{Binding DisplayName}"/>
                                </StackPanel>
                            </DataTemplate>
                        </ComboBox.ItemTemplate>
                    </ComboBox>

                    <!-- Zoom -->
                    <TextBlock Text="文字サイズ (ズーム)" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,4"/>
                    <Grid Margin="0,0,0,16">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="50"/>
                        </Grid.ColumnDefinitions>
                        <Slider x:Name="ZoomSlider" Minimum="50" Maximum="200" TickFrequency="10" IsSnapToTickEnabled="True" ValueChanged="ZoomSlider_ValueChanged"/>
                        <TextBlock x:Name="ZoomValueText" Grid.Column="1" Text="100%" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="{DynamicResource TextBrush}"/>
                    </Grid>

                    <Separator Opacity="0.2" Background="{DynamicResource TextBrush}" Margin="0,8,0,16"/>

                    <!-- Editor Section -->
                    <TextBlock Text="エディタ" FontSize="18" FontWeight="SemiBold" Foreground="{DynamicResource TextBrush}" Margin="0,0,0,12"/>
                    
                    <CheckBox x:Name="MarkdownCheck" Content="Markdown装飾を有効にする" IsChecked="True" Margin="0,0,0,8" Foreground="{DynamicResource TextBrush}"/>
                    <CheckBox x:Name="AutoSaveCheck" Content="自動保存を有効にする" IsChecked="True" Margin="0,0,0,8" Foreground="{DynamicResource TextBrush}"/>
                    
                </StackPanel>
            </ScrollViewer>
            
            <!-- Footer -->
             <Border Grid.Row="2" Padding="16" BorderThickness="0,1,0,0" BorderBrush="#10000000">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                     <Button Content="閉じる" Width="80" Height="30" Click="CloseButton_Click"/>
                </StackPanel>
            </Border>
        </Grid>
    </Border>
</Window>
